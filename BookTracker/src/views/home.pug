extends default

block content
  .container
    include mixins/searchbar
    +searchbar()
    
    h2.subtitle All Books

    if !books || !books.length
        p No books yet. Click "Add book" to start your library!
    else 
      ul.search-results
        include mixins/results
        +results(books)