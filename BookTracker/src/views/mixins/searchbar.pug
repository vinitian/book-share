mixin searchbar(searchQuery)
  form.search(action='/search' method='GET')
    input.search-input(autofocus='' value=`${searchQuery || ''}` placeholder='Search Your Library...' type='search' name='q')
