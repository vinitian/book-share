// TODO: complete the borrow book function
function borrowBook(id) {
  console.log(`Book id ${id} borrowed`);
  //   const response = await axios.get(`${process.env.BOOKSERVICE_URL}/`);
  //   return response.data;
}
