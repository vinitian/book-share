extends default

block content
  .home-search
    h1.title Welcome to Book Share!
    form(action='/search' method='GET')
      input.home-input(autofocus='' placeholder='Search Book Share' type='search' name='q')

  .container
    h2 hiiiiii
    ul.search-results
      include mixins/results
      +results(books)