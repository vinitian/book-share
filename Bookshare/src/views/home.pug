extends default

//- TODO: add "Add book" button that redirects to /add page
block content
  .home-search
    h1.title My Personal Library
    form(action='/search' method='GET')
      input.home-input(autofocus='' placeholder='Search Your Library...' type='search' name='q')

  .container
    ul.search-results
      include mixins/results
      +results(books)